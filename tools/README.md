# Tools — набор инструментов для LLM

Каждая папка = один инструмент как в NeoStack, но открытый.

LLM вызывает их через function calling / MCP, а не через закрытый плагин.

Смотри `openai-tools.json` для импорта в OpenAI / Claude.
